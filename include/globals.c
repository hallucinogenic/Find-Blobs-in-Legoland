/*
    FICHEIRO DE VARIÁVEIS GLOBAIS UTILIZADAS NO PROGRAMA!
*/

int DEBUG = 0; // Variável apenas para DEBUG;

int counter = 0; // Contador de Píxeis de Cada BLOB que encontrar-mos;
int counter_linhas = 0; // Contador das Linhas dos Píxeis de Cada BLOB que encontrar-mos;
int counter_colunas = 0; // Contador das Colunas dos Píxeis de Cada BLOB que encontrar-mos;